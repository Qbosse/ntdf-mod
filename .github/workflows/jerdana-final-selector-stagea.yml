name: Jerdana Final Selector Menu Pruning Placement

on:
  workflow_dispatch:
  push:
    branches:
      - jerdana-final-selector-menu-pruning-placement

env:
  OFFICIAL_PNACH_SHA256: 8eee249568fd94e05998b0dab30b8fa427e064845dd8b73f9e9f4f25865eb214
  OFFICIAL_ELF_SHA256: 43eba6baafba8aa20bfa2b2c9d8c7e5fcb24eb30ce2d9c92f2bbd4eea8bacf30
  CORRECTED_SOURCE_SHA256: ace19be88030e2955b891ce7061a1ff194939fc6039204957bcea803b8adc15e
  STAGE2_SOURCE_SHA256: e36ca23573c15b5a3676276a4206f255cc9c0eb0c501c1980d2675b49dc38406
  R3_SOURCE_SHA256: 2e593ddec8c804aa96e2e80859606c8de0d75d88564ad109c69e73093cb40ad7
  BUILD1_SOURCE_SHA256: 755db82408e81438fea5e85d36513a980466bd1e749228fd77f1bfc07301368f
  BUILD2_SOURCE_SHA256: a579b8b37bfc24414521856f44794071bb0a94875bac3cc05ae3e56eac9739c7
  BUILD3_SOURCE_SHA256: 36fcee4b477893e9280b2ac84587c8c80231574be0e4faf113ead642a6cebddf
  MENU_PRUNING_SOURCE_SHA256: f7e69e263356e3ac19a996c33aae8f2d45d66449297af91ab2cffe13e8a260f2

jobs:
  official-v2-4-regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Setup official v2.4 PS2DEV environment
        run: |
          sudo apt-get update
          sudo apt-get install -y nodejs
          wget https://github.com/ps2dev/ps2dev/releases/download/v1.1/ps2dev-ubuntu-latest.tar.gz
          sudo tar -xzf ps2dev-ubuntu-latest.tar.gz --directory /usr/local

      - name: Build unmodified official source
        run: |
          export PS2DEV=/usr/local/ps2dev
          export PS2SDK=$PS2DEV/ps2sdk
          export GSKIT=$PS2DEV/gsKit
          export PATH=$PATH:$PS2DEV/bin:$PS2DEV/ee/bin:$PS2DEV/iop/bin:$PS2DEV/dvp/bin:$PS2SDK/bin
          cd mod
          ./build.sh

      - name: Fetch and verify official release artifacts
        run: |
          wget -O reproducibility/official-v2.4-934F9081.pnach https://github.com/monster860/ntdf-mod/releases/download/v2.4/934F9081.pnach
          wget -O reproducibility/official-v2.4-df_hack.elf https://github.com/monster860/ntdf-mod/releases/download/v2.4/df_hack.elf
          test "$(sha256sum reproducibility/official-v2.4-934F9081.pnach | awk '{print $1}')" = "$OFFICIAL_PNACH_SHA256"
          test "$(sha256sum reproducibility/official-v2.4-df_hack.elf | awk '{print $1}')" = "$OFFICIAL_ELF_SHA256"

      - name: Require byte-identical official regression
        run: |
          test "$(sha256sum mod/934F9081.pnach | awk '{print $1}')" = "$OFFICIAL_PNACH_SHA256"
          test "$(sha256sum mod/df_hack.elf | awk '{print $1}')" = "$OFFICIAL_ELF_SHA256"
          test "$(wc -l < mod/934F9081.pnach)" -eq 3914
          node reproducibility/verify-r-mips26.js mod/mod.o 0x3043B0 mod/934F9081.pnach reproducibility/official-r-mips26-report.txt memset=0x2BFE8C
          node scripts/compare-pnach-layout.js reproducibility/official-v2.4-934F9081.pnach mod/934F9081.pnach | tee reproducibility/official-layout-report.txt
          {
            echo "official_regression=pass"
            sha256sum mod/934F9081.pnach mod/df_hack.elf mod/loader/patch.c mod/mod.ii mod/mod.s mod/mod.o elf_processor.js reproducibility/verify-r-mips26.js
            wc -l mod/934F9081.pnach
          } | tee reproducibility/official-regression-report.txt

      - name: Preserve official regression source state
        if: always()
        run: |
          git diff -- elf_processor.js mod/mod.cpp mod/make_mod.js mod/build.sh > reproducibility/official-source-diff.patch
          git status --short > reproducibility/official-git-status.txt
          git rev-parse HEAD > reproducibility/source-commit.txt

      - name: Upload official regression evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: official-v2.4-r3-multitarget-regression
          path: |
            mod/934F9081.pnach
            mod/df_hack.elf
            mod/mod.ii
            mod/mod.s
            mod/mod.o
            mod/loader/patch.c
            reproducibility/official-v2.4-934F9081.pnach
            reproducibility/official-v2.4-df_hack.elf
            reproducibility/official-r-mips26-report.txt
            reproducibility/official-layout-report.txt
            reproducibility/official-regression-report.txt
            reproducibility/official-source-diff.patch
            reproducibility/official-git-status.txt
            reproducibility/source-commit.txt
            reproducibility/verify-r-mips26.js
            elf_processor.js

  final-selector-stagea:
    needs: official-v2-4-regression
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Stage official regression proof for the Stage-A verifier
        uses: actions/download-artifact@v4
        with:
          name: official-v2.4-r3-multitarget-regression
          path: reproducibility/official-v2.4-regression

      - name: Setup official v2.4 PS2DEV environment
        run: |
          sudo apt-get update
          sudo apt-get install -y nodejs unzip
          wget https://github.com/ps2dev/ps2dev/releases/download/v1.1/ps2dev-ubuntu-latest.tar.gz
          sudo tar -xzf ps2dev-ubuntu-latest.tar.gz --directory /usr/local

      - name: Reconstruct Build-3 parent and apply the menu-pruning placement layer
        run: |
          git apply --ignore-whitespace --check reproducibility/jerdana-stage1-hardcoded-downgrade-shrink-pass3.patch
          git apply --ignore-whitespace reproducibility/jerdana-stage1-hardcoded-downgrade-shrink-pass3.patch
          test "$(sha256sum mod/mod.cpp | awk '{print $1}')" = "$CORRECTED_SOURCE_SHA256"
          git apply --ignore-whitespace --check reproducibility/jerdana-stage2-preview-safety-readback.patch
          git apply --ignore-whitespace reproducibility/jerdana-stage2-preview-safety-readback.patch
          test "$(sha256sum mod/mod.cpp | awk '{print $1}')" = "$STAGE2_SOURCE_SHA256"
          git apply --ignore-whitespace --check reproducibility/jerdana-r3-multitarget-diagnostic.patch
          git apply --ignore-whitespace reproducibility/jerdana-r3-multitarget-diagnostic.patch
          test "$(sha256sum mod/mod.cpp | awk '{print $1}')" = "$R3_SOURCE_SHA256"
          mkdir -p reproducibility/previous/r3-source
          cp mod/mod.cpp reproducibility/previous/r3-source/mod.cpp
          git apply --ignore-whitespace --check reproducibility/jerdana-final-selector-stagea.patch
          git apply --ignore-whitespace reproducibility/jerdana-final-selector-stagea.patch
          test "$(sha256sum mod/mod.cpp | awk '{print $1}')" = "$BUILD1_SOURCE_SHA256"
          mkdir -p reproducibility/previous/build1-source
          cp mod/mod.cpp reproducibility/previous/build1-source/mod.cpp
          git apply --ignore-whitespace --check reproducibility/jerdana-final-selector-stagea-byte-recovery.patch
          git apply --ignore-whitespace reproducibility/jerdana-final-selector-stagea-byte-recovery.patch
          test "$(sha256sum mod/mod.cpp | awk '{print $1}')" = "$BUILD2_SOURCE_SHA256"
          mkdir -p reproducibility/previous/build2-source
          cp mod/mod.cpp reproducibility/previous/build2-source/mod.cpp
          git apply --ignore-whitespace --check reproducibility/jerdana-final-selector-stagea-build3-tier1b.patch
          git apply --ignore-whitespace reproducibility/jerdana-final-selector-stagea-build3-tier1b.patch
          test "$(sha256sum mod/mod.cpp | awk '{print $1}')" = "$BUILD3_SOURCE_SHA256"
          mkdir -p reproducibility/previous/build3-source
          cp mod/mod.cpp reproducibility/previous/build3-source/mod.cpp
          git apply --ignore-whitespace --check reproducibility/jerdana-final-selector-menu-pruning-placement.patch
          git apply --ignore-whitespace reproducibility/jerdana-final-selector-menu-pruning-placement.patch
          test "$(sha256sum mod/mod.cpp | awk '{print $1}')" = "$MENU_PRUNING_SOURCE_SHA256"
          diff -u reproducibility/previous/r3-source/mod.cpp mod/mod.cpp > reproducibility/source-diff-vs-r3-parent.patch || test $? -eq 1
          diff -u reproducibility/previous/build1-source/mod.cpp mod/mod.cpp > reproducibility/source-diff-vs-build1-parent.patch || test $? -eq 1
          diff -u reproducibility/previous/build2-source/mod.cpp mod/mod.cpp > reproducibility/source-diff-vs-build2-parent.patch || test $? -eq 1
          diff -u reproducibility/previous/build3-source/mod.cpp mod/mod.cpp > reproducibility/source-diff-vs-build3-parent.patch || test $? -eq 1
          git diff --stat -- reproducibility/jerdana-final-selector-stagea.patch reproducibility/jerdana-final-selector-stagea-byte-recovery.patch reproducibility/jerdana-final-selector-stagea-build3-tier1b.patch reproducibility/jerdana-final-selector-menu-pruning-placement.patch mod/mod.cpp > reproducibility/stagea-source-scope.txt

      - name: Make official regression report available to verifier
        run: |
          test -f reproducibility/official-v2.4-regression/reproducibility/official-regression-report.txt
          cp reproducibility/official-v2.4-regression/reproducibility/official-regression-report.txt reproducibility/official-regression-report.txt
          grep -Fx "official_regression=pass" reproducibility/official-regression-report.txt

      - name: Build final selector menu-pruning placement candidate
        run: |
          export PS2DEV=/usr/local/ps2dev
          export PS2SDK=$PS2DEV/ps2sdk
          export GSKIT=$PS2DEV/gsKit
          export PATH=$PATH:$PS2DEV/bin:$PS2DEV/ee/bin:$PS2DEV/iop/bin:$PS2DEV/dvp/bin:$PS2SDK/bin
          cd mod
          ./build.sh

      - name: Fetch official baseline
        run: |
          wget -O reproducibility/official-v2.4-934F9081.pnach https://github.com/monster860/ntdf-mod/releases/download/v2.4/934F9081.pnach
          test "$(sha256sum reproducibility/official-v2.4-934F9081.pnach | awk '{print $1}')" = "$OFFICIAL_PNACH_SHA256"

      - name: Preserve generated disassembly and symbol sizes
        run: |
          export PS2DEV=/usr/local/ps2dev
          export PATH=$PATH:$PS2DEV/ee/bin
          ee-objdump -dr mod/mod.o > reproducibility/mod-objdump-dr.txt
          ee-readelf -rWs mod/mod.o > reproducibility/mod-readelf-rws.txt
          {
            echo "candidate_symbols:"
            ee-nm -S mod/mod.o | grep -E 'stage1_action|ResumeGameHook|outfit_sel|HandleResumeGameTrampoline' || true
          } | tee reproducibility/symbol-size-report.txt

      - name: Verify relocations, generated safety semantics, hooks, and layout
        run: |
          node reproducibility/verify-r-mips26.js mod/mod.o 0x3043B0 mod/934F9081.pnach reproducibility/candidate-r-mips26-report.txt memset=0x2BFE8C
          node reproducibility/verify-final-selector-stagea.js
          node scripts/compare-pnach-layout.js reproducibility/official-v2.4-934F9081.pnach mod/934F9081.pnach | tee reproducibility/layout-vs-official.txt

      - name: Record candidate artifact identities
        run: |
          {
            echo "build_environment=GitHub Actions official v2.4 PS2DEV v1.1"
            echo "build2_source=${BUILD2_SOURCE_SHA256}"
            echo "build3_source=${BUILD3_SOURCE_SHA256}"
            echo "menu_pruning_source=${MENU_PRUNING_SOURCE_SHA256}"
            echo "authorized_change=Placement-A selector relocation, direct Show Coordinates, and Tier-1 legacy-surface removal"
            echo "R_MIPS_26_fix_preserved=yes"
            echo "installed=no"
            echo "runtime_tested=no"
            sha256sum mod/mod.cpp mod/934F9081.pnach mod/df_hack.elf mod/loader/patch.c mod/mod.ii mod/mod.s mod/mod.o elf_processor.js reproducibility/jerdana-final-selector-stagea.patch reproducibility/jerdana-final-selector-stagea-byte-recovery.patch reproducibility/jerdana-final-selector-stagea-build3-tier1b.patch reproducibility/jerdana-final-selector-menu-pruning-placement.patch reproducibility/verify-r-mips26.js reproducibility/verify-final-selector-stagea.js
            wc -l mod/934F9081.pnach
          } | tee reproducibility/candidate-hash-report.txt

      - name: Preserve final source state
        if: always()
        run: |
          git diff -- elf_processor.js mod/mod.cpp mod/make_mod.js mod/build.sh reproducibility/jerdana-final-selector-stagea.patch reproducibility/jerdana-final-selector-stagea-byte-recovery.patch reproducibility/jerdana-final-selector-stagea-build3-tier1b.patch reproducibility/jerdana-final-selector-menu-pruning-placement.patch reproducibility/verify-final-selector-stagea.js .github/workflows/jerdana-final-selector-stagea.yml > reproducibility/candidate-source-diff.patch
          git status --short > reproducibility/candidate-git-status.txt
          git rev-parse HEAD > reproducibility/source-commit.txt

      - name: Upload final selector menu-pruning placement evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jerdana-final-selector-menu-pruning-placement
          path: |
            mod/934F9081.pnach
            mod/df_hack.elf
            mod/mod.cpp
            mod/mod.ii
            mod/mod.s
            mod/mod.o
            mod/loader/patch.c
            reproducibility/jerdana-stage1-hardcoded-downgrade-shrink-pass3.patch
            reproducibility/jerdana-stage2-preview-safety-readback.patch
            reproducibility/jerdana-r3-multitarget-diagnostic.patch
            reproducibility/jerdana-final-selector-stagea.patch
            reproducibility/jerdana-final-selector-stagea-byte-recovery.patch
            reproducibility/jerdana-final-selector-stagea-build3-tier1b.patch
            reproducibility/jerdana-final-selector-menu-pruning-placement.patch
            reproducibility/source-diff-vs-r3-parent.patch
            reproducibility/source-diff-vs-build1-parent.patch
            reproducibility/source-diff-vs-build2-parent.patch
            reproducibility/source-diff-vs-build3-parent.patch
            reproducibility/stagea-source-scope.txt
            reproducibility/official-regression-report.txt
            reproducibility/candidate-r-mips26-report.txt
            reproducibility/final-selector-menu-pruning-placement-gate-report.txt
            reproducibility/dead-hold-dominance-report.txt
            reproducibility/source-isolation-report.txt
            reproducibility/stageb-absence-report.txt
            reproducibility/store-provenance-report.txt
            reproducibility/get-current-equipment-slot-report.txt
            reproducibility/hook-address-report.txt
            reproducibility/hook-target-report.txt
            reproducibility/layout-vs-official.txt
            reproducibility/candidate-hash-report.txt
            reproducibility/mod-objdump-dr.txt
            reproducibility/mod-readelf-rws.txt
            reproducibility/symbol-size-report.txt
            reproducibility/candidate-source-diff.patch
            reproducibility/candidate-git-status.txt
            reproducibility/source-commit.txt
            reproducibility/verify-r-mips26.js
            reproducibility/verify-final-selector-stagea.js
            .github/workflows/jerdana-final-selector-stagea.yml
            elf_processor.js
